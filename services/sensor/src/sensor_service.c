#include "sensor_service.h"

#include "esp_log.h"

static const char *TAG = "sensor_service";

esp_err_t sensor_service_init(void)
{
    ESP_LOGI(TAG, "Initialize sensor service placeholder");
    return ESP_OK;
}
