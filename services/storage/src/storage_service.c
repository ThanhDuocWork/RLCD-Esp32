#include "storage_service.h"

#include "esp_log.h"

static const char *TAG = "storage_service";

esp_err_t storage_service_init(void)
{
    ESP_LOGI(TAG, "Initialize storage service placeholder");
    return ESP_OK;
}
