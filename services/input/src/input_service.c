#include "input_service.h"

#include "esp_log.h"

static const char *TAG = "input_service";

esp_err_t input_service_init(void)
{
    ESP_LOGI(TAG, "Initialize input service placeholder");
    return ESP_OK;
}
