#include "audio_service.h"

#include "esp_log.h"

static const char *TAG = "audio_service";

esp_err_t audio_service_init(void)
{
    ESP_LOGI(TAG, "Initialize audio service placeholder");
    return ESP_OK;
}
